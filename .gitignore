# OS
.DS_Store

# Python
__pycache__/
*.pyc
.venv/

# Jupyter
.ipynb_checkpoints/

# transient files regenerated from the nxbase API at run time
support/_nxbase_*.csv

# data does not live in git — trades workbooks are Electricity Maps' proprietary
# ToS data (non-redistributable); they live in the OneDrive archive
# (Databases/nxsut/_trades_data/) and are governed in nxbase (electricity_maps/)
support/trades_*.xlsx

# transient add_sectors master (rebuilt from the nxbase recipe at run time)
_steel_master.xlsx

# internal-only generators (v1/v2 depend on the proprietary Electricity Maps trade
# mix — not redistributable): the public repo ships the open v3.0 pipeline only.
gen_v1.ipynb
gen_v2.ipynb

# internal working plan / roadmap — not public
UPDATE_PLAN.md

# superseded generators (also on the proprietary mix); kept locally, out of the repo
_old/

# personal, un-sanitized paths override (paths.yml is the public template)
paths_personal.yml

# stray scratch file, kept locally
fp hyb_vs_f eco.py
support/wp1_perimeter.csv

calc_footprints.ipynb